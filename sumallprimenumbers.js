  // create a function

  // number only divisible by one or two

  // use modulo and return to boolean true or false 

  // create a loop to see if less than or equal to given number
   // two variables to 
   function isPrime(num) {
    for(var i = 2; i < num; i++) {
        if(num % i === 0) 
        }

    }